# fizzbuzz

Implement `fizzbuzz(n)` in `solution.py`.

Return `"Fizz"` when `n` is divisible by 3, `"Buzz"` when divisible by 5,
`"FizzBuzz"` when divisible by both, and otherwise the number as a string.
